# --- CORE FRAMEWORK ---
streamlit
pandas
numpy
requests
python-dotenv

# --- GEOSPATIAL STACK ---
pydeck
geopy
mapbox
geopandas

# --- DEEP LEARNING & VIZ ---
plotly
matplotlib
scikit-learn
# If using PyTorch (recommend unpinned for CI/CD compatibility)
torch
torchvision

# --- API & BACKEND ---
fastapi
uvicorn
pydantic
python-multipart

# --- CROSS-PLATFORM FIXES ---
# This ensures GitHub (Linux) skips the Windows-only library
pywin32; sys_platform == 'win32'

# Relaxed versioning for problematic packages
alembic
contourpy
attrs
jsonschema